---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/layout/Header.astro';
import Footer from '../components/layout/Footer.astro';
import SearchResults from '../components/sections/SearchResults';
import { findAllLibros } from '../lib/repos/libros.js';
import { findAllRevistas } from '../lib/repos/revistas.js';

export const prerender = false;

const [libros, revistas] = await Promise.all([
  findAllLibros({ size: 1000 }),
  findAllRevistas({ size: 1000 }),
]);
---

<BaseLayout title="Buscar — Sapiens">
  <Header />
  <main class="max-w-[1280px] mx-auto px-6 py-10">
    <h1 class="font-playfair font-bold text-3xl text-text mb-6">Buscar</h1>
    <SearchResults libros={libros} revistas={revistas} client:load />
  </main>
  <Footer />
</BaseLayout>
